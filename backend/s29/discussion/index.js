let countries = ['US', 'PH', 'CAN', 'SG', 'TH', 'PH', 'FR', 'DE'];

// indexOf() returns the index of the 

// map() vs forEach()
// forEach() only iterates
// map() iterates AND returns new set of values depending on the operation

